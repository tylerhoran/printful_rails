require "spec_helper"

RSpec.describe PrintfulRails do
  it "has a version number" do
    expect(PrintfulRails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
